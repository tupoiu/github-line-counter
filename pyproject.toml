[project]
name = "github-line-counter"
version = "0.1.0"
description = "Webapp for counting lines of a public github repo"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.2",
    "gunicorn>=23.0.0",
]
