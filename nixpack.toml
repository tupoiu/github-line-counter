# OR use nix packages (recommended)
[nix]
packages = ["git"]

[phases.build]
cmds = [
    "uv sync --frozen",  # Install Python dependencies
    "git --version"      # Verify git is available
]

[phases.start]
cmd = "uv run gunicorn app:app"